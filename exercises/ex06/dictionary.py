"""Dictionary program as apart of ex06 exercises."""

__author__ = "730316240"


def invert(original: dict[str, str]) -> dict[str, str]:
    """Creates a dictionary program that will invert the key and value."""
    inverted: dict[str, str] = {} 
    for item in inverted:
        if item == inverted[item]:
            raise KeyError(f"Key values are the same.")
        else:
            inverted[original["inverted"]] = original[key] 
    return inverted


def favorite_color(color: dict[str, str]) -> str:
    """Creates a dictionary program that will return the most used color value."""
    output: str = ""
    for o in color:
        if 
    return output


def count(c_list: list[str]) -> dict[str, int]:
    """Creates a dictionary that will count the number of times a value appears in a dictionary."""
    c_dict: dict[str, int] = {}
    for c in c_list:
        if 
    return c_dict
